#include <bits/stdc++.h>
using namespace std;

int main()
{

    // numeric data types: =
    // int, long, long long, double , float

    // int x,y;
    // cin>>x>>y;

    // cout<<"value of x:" <<x<<"\n"<<"and y:" <<y;
    // long x;
    // long long x; //these are used to store large no.

    // float, double
    float x = 5.6;
    float y = 5;
    cout << "value of y: " << y<<endl;

    // string and getline
    string str;
    getline(cin,str);
    cout<<"string is = " <<str<<endl;
    //char  is used for storing single char ( as string takes more space)

    char c =  'b';
    cout<< c;

    return 0;
}